export interface ChildProps {
  children: React.ReactNode;
}

// Blogs interface
export interface IBlog {
  title: string;
  description: string;
  author: string;
  date: string;
  image: string;
  tags: string[];
}
